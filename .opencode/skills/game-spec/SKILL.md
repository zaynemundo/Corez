---
name: game-spec
description: Produces validated game-spec.json containing core loops, mechanics, player controls, enemy types, and win/loss conditions.
---

# Game Spec Skill

Formulates structured data in `game-project/design/game-spec.json` ensuring required fields (`title`, `genre`, `coreLoop`, `controls`, `winCondition`, `loseCondition`) are fully defined.
